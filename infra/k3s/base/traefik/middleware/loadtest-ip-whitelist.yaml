# 부하테스트 IP 화이트리스트 — Cloudflare Access와 이중 방어 구성용
# 적용 전 YOUR_IP/32를 실제 IP로 교체하여 사용할 수 있음
# IP 확인: curl -s ifconfig.me
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: loadtest-ip-whitelist
spec:
  ipAllowList:
    sourceRange:
      - "YOUR_IP/32"
