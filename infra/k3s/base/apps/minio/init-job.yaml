apiVersion: batch/v1
kind: Job
metadata:
  name: minio-init
spec:
  ttlSecondsAfterFinished: 300
  backoffLimit: 3
  template:
    metadata:
      labels:
        app: minio-init
    spec:
      automountServiceAccountToken: false
      restartPolicy: Never
      initContainers:
        - name: wait-for-minio
          image: busybox:1.37.0
          command: [ "sh", "-c", "until nc -z minio 9000; do echo 'waiting for minio...'; sleep 2; done" ]
      containers:
        - name: mc
          image: minio/mc:RELEASE.2025-08-13T08-35-41Z
          command:
            - /bin/sh
            - -c
            - |
              mc alias set local http://minio:9000 "$MINIO_ROOT_USER" "$MINIO_ROOT_PASSWORD"
              mc mb --ignore-existing local/giftify-images
              mc mb --ignore-existing local/giftify-logs
              mc ilm rule add --expire-days 30 local/giftify-logs
          envFrom:
            - secretRef:
                name: minio-secrets
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "128Mi"
              cpu: "100m"
