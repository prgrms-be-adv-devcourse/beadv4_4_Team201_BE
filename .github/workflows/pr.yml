# PR Pipeline
# PR 생성/업데이트 시 코드 품질 검증 및 리뷰 지원
name: PR Pipeline

on:
  pull_request:
    branches: [ main, develop ]
    types: [ opened, synchronize, reopened ]

jobs:
  # 1. 빌드 및 테스트 (Strict Mode)
  build:
    name: Build & Test
    uses: ./.github/workflows/_build.yml
    with:
      java-version: '21'
      run-tests: true
      # PR 단계에서는 커버리지 필수
      generate-coverage: true
      # PR에서는 jar 불필요
      upload-jar: false
      publish-build-scan: true

  # 2. 커버리지 리포트 PR 코멘트
  coverage-report:
    name: Coverage Report
    needs: [ build ]
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
      contents: read
    steps:
      - name: Download Jacoco report
        uses: actions/download-artifact@v4
        with:
          name: jacoco-report
          path: jacoco-report

      - name: Add Coverage PR Comment
        uses: madrapps/jacoco-report@v1.7.1
        with:
          paths: jacoco-report/jacocoTestReport.xml
          token: ${{ secrets.GITHUB_TOKEN }}
          min-coverage-overall: 40
          min-coverage-changed-files: 60
          title: "## 테스트 커버리지 리포트"
          update-comment: true

  # 3. SonarCloud 정적 분석 (선택적)
  sonarcloud:
    name: SonarCloud
    needs: [ build ]
    runs-on: ubuntu-latest
    # SonarCloud 설정이 완료된 경우에만 실행
    if: vars.SONARCLOUD_ENABLED == 'true'
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Download Jacoco report
        uses: actions/download-artifact@v4
        with:
          name: jacoco-report
          path: jacoco-report

      - name: Set SonarCloud Project Key
        run: |
          REPO_NAME=$(echo $GITHUB_REPOSITORY | cut -d '/' -f 2)
          ORG_NAME=$(echo $GITHUB_REPOSITORY | cut -d '/' -f 1)
          echo "SONAR_PROJECT_KEY=${ORG_NAME}_${REPO_NAME}" >> $GITHUB_ENV

      - name: Analyze with SonarCloud
        uses: SonarSource/sonarcloud-github-action@master
        continue-on-error: true
        env:
          GITHUB_TOKEN: ${{ secrets.SECRET_GITHUB_BOT }}
          SONAR_TOKEN: ${{ secrets.SECRET_SONARQUBE }}
        with:
          args: |
            -Dsonar.projectKey=${{ env.SONAR_PROJECT_KEY }}
            -Dsonar.organization=f-lab-edu-1

  # 4. 자동 리뷰어 할당
  auto-assign:
    name: Auto Assign
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Auto Assign Reviewers
        uses: kentaro-m/auto-assign-action@v2.0.0
        continue-on-error: true
        with:
          configuration-path: '.github/auto-assign.yml'
