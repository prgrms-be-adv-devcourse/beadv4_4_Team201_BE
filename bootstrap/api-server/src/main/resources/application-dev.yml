# 개발 환경 (docker-compose, 개발 서버)
# PostgreSQL 사용, 환경변수로 접속 정보 주입
spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/giftify_db}
    username: ${DB_USERNAME:giftify}
    password: ${DB_PASSWORD:secret}
    driver-class-name: org.postgresql.Driver

  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD}
      timeout: 2000ms

  batch:
    jdbc:
      initialize-schema: always   # Batch 메타데이터 테이블 자동 생성
    job:
      enabled: false              # 기동 시 배치 잡 자동 실행 방지

  h2:
    console:
      enabled: false

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        default_schema: g7app

  flyway:
    enabled: true
    baseline-on-migrate: true
    baseline-version: "0"
    locations: classpath:db/migration
    schemas: g7app
    default-schema: g7app
    create-schemas: true

  sql:
    init:
      mode: never

management:
  metrics:
    tags:
      application: ${spring.application.name:giftify}
      env: dev
